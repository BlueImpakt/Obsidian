---
tags: [daily]
created: 2026-09-11
---

# 2026-09-11

## Pense-bêtes

- [ ]

## Ce qu'on a fait

- naeco-carte : bug de création de site d'étude corrigé (clic de placement intercepté par les polygones AMP/Pelagos/markers), poussé (`7bc57e6`) — 16 sites d'étude vides nettoyés du JSONbin au passage
- naeco-carte : bug photo escale Ajaccio (fenêtre noire) corrigé — lien de partage Google Drive converti en URL image directe via `gdriveImg()`, commit `b209418` pas encore poussé
- naeco-carte : nouveau site d'étude Point Zéro ajouté (« Prélèvement microplastique », au large de Galéria, 11/09 09h00)
- naeco-carte : commit `b209418` (fix photo Google Drive escale Ajaccio) poussé sur `main`
- naeco-carte : galerie multi-photos implémentée — clic sur une photo (site d'étude/observation/escale) ouvre une visionneuse plein écran (80vw/80vh), navigation flèches gauche/droite + clavier, compteur ; champ `photo` unique des escales migré vers `media[]` (multi-photos, migration transparente) ; poussé (`20cfcba`)
- naeco-carte : marqueurs photo indépendants des observations implémentés (nouvel onglet « Photo » dans l'éditeur, pin goutte violet, clic sur la carte pour placer, position + reverse-géocodage auto, plusieurs photos possibles, galerie plein écran réutilisée), poussé (`8a72672`)
- naeco-carte : formulaire photo simplifié sur retour client — retrait des champs Expédition associée/Description, popup carte n'affiche plus que la ou les photos, champ Nom devient un repère interne éditeur uniquement, poussé (`af145a4`)
- naeco-carte : faille de sécurité identifiée — mot de passe éditeur et clé JSONbin (lecture/écriture complète sur les données de production) tous deux en clair dans le code source, accessibles via « Afficher le code source »
- naeco-carte : root cause du flash de la légende (fix probabiliste `8118cbd` jamais résolu) identifiée — calque `.lp` isolé par `contain:layout style paint` présenté à ses bornes périmées (largeur du panneau vide, 34px) car une animation `stroke-dashoffset` (`dash-flow`) morte (propriété SVG appliquée par erreur à une `<div>`) forçait un repaint continu ; corrigé (retrait du `contain`), poussé avec le fix Photos (`8a72672`)
- naeco-carte : second flash découvert sur la carte elle-même (bande de tuiles rastérisées périmées, 23% des frames, colonne fixe x=798→1168) — même cause `dash-flow`/`stroke-dashoffset`, cette fois sur le `<svg>` partagé entre la trace pointillée et le polygone Pelagos (`fill-opacity:0.15`) ; animation passée à `steps(8)` (~5,7 repaints/s au lieu de ~60), poussé (`af145a4..6f98227`)
- naeco-carte : proxy Cloudflare Worker JSONbin déployé et vérifié en prod (`/api/login`, `/api/sync`, `/api/data`) — mot de passe éditeur et clé JSONbin maître retirés du code client ; la clé JSONbin "lecture seule" créée s'est révélée rejetée par l'API JSONbin (bug côté JSONbin), donc la lecture aussi routée via le Worker : plus aucune clé JSONbin côté client. Testé en prod (login rejette mauvais mdp, sync refuse écriture non authentifiée, data retourne les vraies données), confirmé fonctionnel par le client
- naeco-carte : vignette principale retirée des popups "sites d'étude" (photos visibles uniquement via la petite galerie), poussé (`9a002e6`)
- naeco-carte : onglet de filtre "Photos" retiré du header (photos toujours visibles sur la carte, plus de filtrage possible), poussé (`b14b832`)
- naeco-carte : nouvelle escale "Point Zéro" créée pour Ota le 11/09/2026 (projection du film MARE NOSTRUM + ateliers de sensibilisation avec le Parc Régional de Corse et STARESO, 6 photos), patch JSONbin direct
- naeco-carte : flash carte toujours présent malgré `steps(8)` — animation des pointillés d'abord coupée, puis essai d'isoler la trace sur son propre calque SVG (`tracesPane`) séparé du polygone Pelagos, animation réactivée sans partage de calque ; au passage, erreur de commit mixé avec le refactor sécurité en cours détectée et rattrapée avant tout push. Poussé (`93e95e8`, puis `78f36e3`)
- naeco-carte : escale Porto disparue de la carte/éditeur — root cause diagnostiquée : `jbSet()` (`index.html:602-611`) pousse **tout le tableau `escales` local** à chaque sauvegarde JSONbin au lieu de patcher uniquement les champs modifiés (viole la règle déjà actée « ne jamais pousser les données complètes »), combiné à la race condition connue (fetch distant asynchrone au chargement) : une modif faite depuis un device/navigateur dont le localStorage ne connaissait pas encore Porto a écrasé la version JSONbin qui l'avait. Aucune trace en Git (l'escale n'a jamais été committée, créée uniquement via l'éditeur live) ni dans l'historique JSONbin (bin sans versioning activé, aucun retour arrière possible). Escale retrouvée après passage sur Opus ; vérification en cours (modif ultérieure éventuelle, contenu exact du scratchpad de session) à la clôture de la session.
- naeco-site : bandeau rouge « Site en chantier » (`#D6362A`, Space Mono 700, style eyebrow) ajouté dans le header entre le logo et les liens, sur les 7 pages live (index, l-association, agir-ensemble, partenaires, expeditions, mobilite, campagne-oceanographique) — vérifié desktop + mobile, aucun chevauchement avec la nav, rien commité
- naeco-carte : escale Ota/Point Zéro perdue (« Porto ») retrouvée intégralement — contenu restauré depuis `verify.json` (réponse serveur byte-exacte conservée dans le scratchpad de la session précédente), payload vérifié (0 caractère corrompu, aucun autre champ touché), snapshot pré-restauration sauvegardé pour rollback, poussé sur JSONbin — 3 escales confirmées en ligne dont Ota avec ses 6 photos
- naeco-carte : bug `jbSet()` corrigé définitivement — dirty tracking déplacé au niveau de `ls_set(key, arr)` (payload partiel envoyé au lieu du tableau `escales` complet) + garde-fou côté Worker `/api/sync` (merge serveur au lieu d'écraser, même si un client bugué renvoie un jour l'état complet), ancien flag `localDirty` retiré ; vérifié en local avant proposition de déploiement

## Décisions prises

- naeco-carte : icône « pin goutte » (pointe vers le bas) retenue pour les futurs marqueurs photo hors observations, parmi 4 styles proposés en visuel
- naeco-carte : fix sécurité retenu — restreindre la clé JSONbin publique en lecture seule + proxy Cloudflare Worker pour l'écriture (même pattern que `naeco-track`), accès éditeur (mot de passe, UI) inchangé côté utilisateur
- naeco-carte : architecture finale retenue pour la sécurité JSONbin — lecture ET écriture routées via le Worker Cloudflare (`/api/data`, `/api/sync`, `/api/login`), abandon de la clé JSONbin "lecture seule" côté client (rejetée par un bug de l'API JSONbin) ; plus aucune clé JSONbin exposée au client

## Next actions

- [ ] naeco-carte : vérifier sur le terrain (vidéo écran réelle) que le flash carte a bien disparu après l'isolation de la trace sur son propre calque SVG (`78f36e3`) — non vérifiable depuis l'environnement de dev
- [ ] naeco-carte : clarifier l'usage des 4 liens Google Drive collés en fin d'une session interrompue avant d'en préciser la demande (probablement une nouvelle photo ou escale à ajouter)

## Blocages

-

## Liens

- [[naeco-carte]]
- [[naeco]]

## Notes vrac

- Patterns ajoutés à `03_knowledge/troubleshooting.md` (naeco-carte) : diagnostic de flash visuel sans repro à la demande (frames vidéo + modèle de calque manquant sur les pixels), isolation d'une animation SVG coûteuse sur un pane/renderer Leaflet dédié, et séparation d'un commit accidentellement mélangé avec le travail non commité d'une session concurrente.
