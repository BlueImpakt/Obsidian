---
tags: [project, encours, naeco]
created: 2026-08-29
statut: encours
client: naeco
---

# naeco-site

## Objectifs du projet
- Site vitrine NAECO : "Mobiliser l'art et la science pour l'océan"
- Présenter les expéditions à la voile, créations artistiques, actions de sensibilisation

## Livrables
- Site public : https://naeco-site.naeco.workers.dev
- Pages : accueil, campagne océanographique, mobilité

## État actuel
- Site en production, déployé sur Cloudflare Pages (auto-déploiement au push `main`)
- **Refonte animée (Phases 1+2 + Approche) déployée en prod le 2026-09-04** (`git push origin master`, 39 commits) — vérifié en ligne (marqueurs `resurgence`/`exp-badge`/`piliers-volets`/`flip-inner` présents, ~213 Ko)

## Décisions techniques
- **Stack** : fichier HTML unique (`index.html`), pas de framework
- **Contenu live-éditable** : un éditeur en ligne (raccourci Ctrl+Shift+E, protégé par mot de passe — voir gestionnaire de secrets du client, pas stocké ici) permet à l'équipe NAECO de modifier textes/images/liens/couleurs sans toucher au code. Les changements sont synchronisés vers **JSONbin** (bin privé) avec debounce ~600ms.
- **Règle de travail critique** : toujours lancer `check-remote.sh` en début de session pour récupérer l'état actuel du contenu depuis JSONbin avant toute modification — sinon risque d'écraser des changements faits par l'équipe via l'éditeur
- **Déploiement** : `sync.sh "description"` → commit + push → Cloudflare Pages redéploie automatiquement
- ⚠️ Le repo client contient une clé maître JSONbin et un mot de passe éditeur en clair dans son propre `CLAUDE.md` — **volontairement non recopiés ici**, à récupérer directement depuis le repo/le client si besoin
- **Ordre des sections verrouillé côté code** (2026-09-03) : l'équipe NAECO ne peut plus réorganiser les sections via l'éditeur (drag désactivé, `sectionOrder` plus lu/écrit dans le bin, `arrange()` fait foi)
- **Vidéos de fond Cloudinary** (2026-09-04) : toujours livrer des fichiers pré-encodés (2 résolutions desktop/mobile), jamais d'URL de transformation à la volée (`f_auto,q_auto,w_...`) — sur ce compte, ces URLs tronquent la durée des vidéos livrées (bug vécu : un film de 23 min ressortait à 4,5s)

## Blocages / risques
- Repo GitHub source de vérité ambigu : le `CLAUDE.md` du projet référence `melp-cloud/naeco-site`, alors que le clone a été fait depuis `NAECOEXPEDITION/naeco-site` — à clarifier lequel est le remote actif avant de pousser du code
- 🔴 **Sécurité** : clé maître JSONbin + mot de passe éditeur stockés en clair dans le `CLAUDE.md` du repo client (pas juste référencés — la valeur elle-même). Si le repo fuite ou si l'accès change de mains, ces secrets sont exposés tels quels.

## Next actions
- [ ] Signaler à NAECO l'exposition en clair de la clé maître JSONbin + mot de passe éditeur dans leur `CLAUDE.md`, recommander de les déplacer vers un gestionnaire de secrets (variable d'environnement, coffre-fort partagé) et de les faire tourner par précaution
- [ ] Câbler les fichiers média réels dans les slots `pilier-1..3`, `team-*`, `carnet-1..5`, `approche-1..3` quand NAECO les fournit
- [ ] Vérifier dans un vrai navigateur le feel des animations scroll (Hero/Manifeste/résurgence migrés vers Motion, sticky-scroll Carnet de bord et Approche) — le pane d'automation gèle `ScrollTimeline`
- [ ] Vérifier en prod (naecoexpedition.org) que l'éditeur JSONbin fonctionne toujours (connexion + test de sauvegarde) après hard-refresh, suite au déploiement de la refonte
- [ ] Trancher la lisibilité au repos des 3 Piliers sans média/hover (options proposées : P1 rendre le contenu visible d'emblée, P2 revenir à 3 cartes distinctes)
- [ ] Retirer `snapshots/naecoexpedition.org_2026-09-03.html` de `master` si préférence de le garder uniquement sur la branche `backup/pre-refonte` (actuellement accessible en prod, `noindex,nofollow`, inoffensif mais superflu)
- [ ] Diagnostic jonctions Hero→Mission / Carnet→Expédition repris avec la technique d'aplat opaque (commits `f523ddb`, `4a180f7`) après échec du correctif root-cause `f719d3f` ; même technique étendue à Constat→Carnet et aux bandes latérales sans voile d'Expédition 2026 (commit `efcc2f2`) — non confirmé visuellement (preview gelée, fenêtre en arrière-plan), à valider par Melvin après hard-refresh
- [ ] Jonction haute Piliers → Approche — appliquer la même technique d'aplat `#0f2536` que Hero → Mission (démarré en fin de session, à vérifier)
- [ ] Pusher les commits locaux en attente (`22ce4a4` vidéo "posidonie poisson", `354fadd` Approche sticky/fondu/voile, `f523ddb`/`4a180f7`/`efcc2f2` jonctions) une fois validés par Melvin
- [ ] Équipe — synchroniser/nettoyer la liste JSONbin `naeco_v5` (retirer le doublon "Marion Fritsch", ajouter le contenu réel du bloc Bureau) avant de considérer la restructuration des cartes Équipe terminée

## Journal
- 2026-08-29 : Repo analysé, fiche projet créée. Pattern JSONbin + éditeur live noté comme knowledge réutilisable.
- 2026-09-01 : Guide de charte graphique complété (polices DM Sans/Inter/Space Mono/Caveat). Couleurs des 3 pages alignées sur la charte NAECO — Bleu Marine `#142A4A` / Ivoire `#F4EEE4`, ratio 60‑30‑10 strict (fond ivoire, cartes+header blancs, accent de pôle par page). Commit+push demandé en fin de session — à confirmer que le check-remote.sh a bien été respecté (voir Next actions daily note du jour).
- 2026-09-01 : Nouvelle page `/expeditions` créée à partir du contenu Notion « Présentation Expédition Point Zéro » (+ sous-page Planning). Contenu : Point Zéro 2026 en page complète (accroche, manifeste, itinéraire, 3 piliers science/art/transmission, partenaires) + « Le sillage » (les 4 expéditions précédentes 2022-2025, vidéos reprises de [[naeco-carte]]). Section `#expedition` de la landing transformée en teaser vers cette page ; onglet menu « Expéditions » redirigé vers `/expeditions` sur les 3 pages du site. Bug de scroll cassé (`overflow-x:hidden` sur `<body>`) corrigé. Patch appliqué au JSONbin en suivant la procédure `CLAUDE.md` du repo (la landing rendait encore du contenu 2025 avant patch).
- 2026-09-01 : Refonte forme éditoriale (style Swiss/magazine, skill `ui-ux-pro-max`) de `expeditions.html` — grille rail+colonne, rail numéroté 01→06, drop cap, pull-quote, trio de chiffres sans fond, palette/fonts du skill ignorées au profit de la charte NAECO verrouillée. Même langage repris sur le teaser `#expedition` de la landing (forme seulement, contenu et liaison JSONbin intactes, tous les `data-id` conservés). Commit+push (`3b75f8c`) sur `NAECOEXPEDITION/naeco-site` après `check-remote.sh` ; 2 fichiers, +382/−342.
- 2026-09-02 : Section HERO retravaillée (crop du bas de l'image, `height` fixe 620px pour supprimer le gap avec la barre de chiffres, `background-position: center top`), bouton « Expédition 2026 » retiré du hero. Barre de chiffres animée (count-up 1,4s au scroll, une fois, désactivée si `prefers-reduced-motion`). Bloc upload vidéo ajouté sous le manifeste/citation de la section Mission (`<video autoplay muted loop playsinline>`, placeholder en attendant le fichier). Commit+push (`237d022`) sur `NAECOEXPEDITION/naeco-site`, 1 fichier +132/−4.
- 2026-09-02 : Brief de refonte éditoriale reçu (abandon du style landing SaaS/puces/définitions dictionnaire, place centrale aux médias photo/vidéo sous-marine, effets de scroll sur le hero — zoom arrière + fondu + flou vidéo de fond, section manifeste en text-reveal au scroll). Le brief supposait une migration React/Next.js/Tailwind/Framer Motion — **décision tranchée : non**, l'architecture HTML statique + éditeur JSONbin (Ctrl+Shift+E) reste en place pour ne pas casser le workflow d'édition de l'équipe NAECO. Animations ajoutées avec **Motion** (lib vanilla JS, ex-Motion One, API `scroll()`/`inView()`/springs compatible Framer Motion mais sans React). Maquettes visuelles (mockups Hero + aperçu scroll, vraie palette/polices) lancées pour trancher les registres d'animation — session interrompue avant le retour des maquettes.
- 2026-09-02 : Retour des 3 maquettes (registres A/B/C) — direction **A** retenue : lumière→profondeur→résurgence. Image Hero conservée telle quelle (haut clair avec header, bas bleu foncé) ; les sections suivantes s'assombrissent progressivement jusqu'au point le plus profond (`#142A4A`, chiffres qui comptent) avant de remonter vers du clair, en une seule descente continue depuis le Hero jusqu'à la résurgence finale.
- 2026-09-02 : Effets de scroll du brief mappés aux valeurs déjà scrapées (pattern [[animations-scroll-gsap]], bac à sable `Scrap landing page` — cantor8/ClearPath) : intro Hero (media fade 1.4-2s, nav stagger 0.08, titre split mots blur+y), zoom/flou vidéo au scroll (scale 1→1.18 scrub), manifeste text-reveal mot à mot (`splitWords`, stagger 0.08, scrub), scroll horizontal + parallaxe pour la galerie Expéditions. Priorité affichée par Melvin : terminer cette refonte avant d'explorer les pistes 3D/scrollpath notées en knowledge (upsells, pas engagées sur ce projet).
- 2026-09-02 : Méthode d'implémentation des animations précisée après inspection du code existant (IntersectionObserver + compteur rAF déjà en place, aucune lib chargée) : JS vanilla en extension de l'infra actuelle retenu pour le Hero/Manifeste (zéro nouvelle dépendance) plutôt que la lib Motion ou GSAP ; GSAP réservé pour plus tard, pour le scroll horizontal du Carnet de bord (Expéditions). Brief de refonte resserré finalisé (5 transformations, scope figé) et input de brainstorming post-refonte préparé pour arbitrer les pistes neuves parkées (voilier 3D scrollpath, médias générés, nouveaux graphiques) une fois la refonte livrée et déployée.
- 2026-09-02 : Spec design Hero + Manifeste (skill brainstorming) finalisée et commitée, non poussée — `docs/superpowers/specs/2026-09-02-hero-manifeste-anime-design.md` sur `master` (repo n'a pas de workflow de branches). Direction « plongée » registre A choisie parmi 3 maquettes visuelles (A lumière→profondeur→résurgence, B contemplatif, C émerveillement lumineux). Spec détaillée : Hero (nouveau `.hero-bg` isolé du contenu, entrée staggerée, scroll zoom/flou/gradient marine), Manifeste (`mh` en Fraunces text-reveal mot à mot avec re-split via MutationObserver sur édition JSONbin, bascule fond ivoire→`#142A4A`), intégration (1 `<style>` + 1 `<script>` vanilla ~80 lignes), `prefers-reduced-motion`, 7 critères de réussite, gouvernance assets générés, roadmap Phases 2-3.
- 2026-09-02 : Brainstorm sur le périmètre neuf parké tranché. **Piste Hero 3D voilier (scrollpath Pelagos)** : hors périmètre de cette refonte (contredit la décision « Hero intouché », coût ~2 semaines, jamais fait chez Blue Impakt) — spec séparée possible plus tard sous condition d'un spike jetable Three.js validant le FPS mobile avant modélisation Blender. **Convergence avec [[naeco-carte]]** : en attente de la piste voilier ; si elle se fait, isoler un module partagé `pelagos-path.js` (courbe + tangente), pas de framework 3D maison avant deux consommateurs réels. **Médias générés IA** : règle de gouvernance adoptée dès maintenant (dernier recours, jamais preuve documentaire scientifique, usage texture/transition basse opacité, taggé dans un manifeste d'assets), rien à produire sur cette livraison. Transition vers le plan d'implémentation (skill writing-plans) démarrée en fin de session.
- 2026-09-02 : Plan d'implémentation Hero+Manifeste rédigé (skill writing-plans, 8 tâches — police Fraunces, calque `.hero-bg`, entrée staggerée, plongée au scroll, restyle Manifeste, `splitWords`+`MutationObserver`, bascule fond+éclairage mot à mot, passe critères de réussite) et sauvegardé dans `docs/superpowers/plans/2026-09-02-hero-manifeste-anime.md`, commité sur `master` (non poussé). Exécution démarrée en mode inline (skill executing-plans) : tâche 1 « police Fraunces + flag `html.js` + token `--font-serif` » en cours.
- 2026-09-02 : Les 8 tâches du plan Hero+Manifeste terminées et commitées sur la branche `phase1-hero-manifeste` (8 commits, `index.html` +250 lignes, nouveau `docs/assets-manifest.md`), vérification automatisée passée (pas de suite de tests sur ce site HTML statique — checks CSSOM/JS/état au repos T1-T7 tous OK). Retour de Melvin : la barre de chiffres doit rester fond marine et suivre directement le Manifeste (pas de fond ivoire, ça casse l'effet plongée). En creusant l'implémentation demandée, découverte que l'ordre des sections vient de `sectionOrder` dans **JSONbin** (piloté par l'équipe NAECO via l'éditeur, pas par le HTML) — en live, Mission est en **9ᵉ position sur 10**, pas du tout adjacente au Hero, ce qui casse le concept de "plongée continue" tel que construit en Phase 1. Correctif (T9/T10) : bloc `.media` (carte vide, le rectangle bleu du screenshot de Melvin) retiré, barre de chiffres repositionnée juste après le Manifeste en fond marine `#142A4A`, ordre `#sections-main` forcé côté code JS au chargement et à chaque réordonnancement éditeur. Effet de bord assumé et documenté : tant que ce code tourne, l'équipe ne peut plus déplacer Mission/chiffres via l'éditeur (revert instantané), et le premier `saveAll()` de l'équipe réécrira `sectionOrder` dans le bin avec Mission en tête. **Incident détecté et corrigé en cours de route** : pendant les tests de vérif du T9, un appel `saveAll()` en console navigateur a sérialisé le DOM déjà découpé par `splitWords()` vers JSONbin, polluant la clé `texts.mh` avec les balises du text-reveal (`<span class="w">…</span>`) — aucun impact visuel en prod (spans sans style = texte inline banal) mais texte canonique corrompu pour les futures éditions/exports. Restauré via un PUT ciblé (vérifié par GET, plus aucune balise), et garde-fou ajouté au code : `saveAll`/`exportHTML` sérialisent désormais `mh` en texte brut le temps de l'écriture puis restaurent les spans — empêche toute pollution future. 10 commits au total sur la branche `phase1-hero-manifeste`. Reste à valider par Melvin dans le navigateur (mouvement au scroll, 60fps, mobile, `prefers-reduced-motion`, édition `Ctrl+Shift+E`) avant merge/déploiement.
- 2026-09-02 : Phase 1 (Hero+Manifeste) validée par Melvin et **mergée dans `master`** (fast-forward, 11 commits, branche `phase1-hero-manifeste` supprimée) — pas encore poussée ni déployée. Brief Phase 2 cadré sur 4 sections indépendantes (Carnet de bord, 3 Piliers, Constat & Chiffres, Équipe & Partenaires) ; 3 des 4 bloquées par l'absence de médias réels (la « banque média complète » annoncée en Phase 1 ne s'est traduite par aucun asset fourni) — décision de poser des placeholders média (slots à insérer plus tard) sur les 4 sections plutôt que d'attendre les fichiers.
- 2026-09-02 : Design Phase 2 figé et validé sur maquette comparative. Ordre de page complet imposé côté code : Hero → Manifeste → Chiffres → Constat → Carnet de bord → 3 Piliers → `#approche` → résurgence (dégradé marine→turquoise→ivoire) → Équipe → Partenaires → Carte → Saint-Ex → Soutenir. `#constat` repensé en 3 chiffres géants (Fraunces, count-up au scroll : 8/10 éco-anxiété, 1% surface océans, ×3 pollution plastique 2040), suppression des puces/glossaire/citation Dostoïevski (arbitrage assumé, réversible). **Changement de techno pour la Phase 2** : passage à **Motion** (lib vanilla JS, CDN, ~5 Ko) pour scroll/count-up/parallaxe/`inView` sur demande explicite de Melvin (« utilise Motion quand c'est la meilleure option ») — lève la contrainte « zéro dépendance » retenue en Phase 1 ; CSS pur conservé pour les volets Piliers (hover `flex-grow`) et la flottaison des bulles Équipe. Spec commitée (non poussée) : `docs/superpowers/specs/2026-09-02-phase2-design.md`. Plan 1/4 (Constat = socle, charge Motion + ordre de page + résurgence, 6 tâches) rédigé et commité — `docs/superpowers/plans/2026-09-02-phase2-constat.md`. Exécution démarrée en mode inline (skill executing-plans).
- 2026-09-02 : **Plan 1/4 (Constat & Chiffres) terminé** et mergé dans `master` (fast-forward, 4 commits, branche `phase2-constat`) : T1 Motion chargé (UMD, `window.Motion`), T2 ordre de page complet forcé par `arrange()` + `<div class="resurgence">` entre `#approche` et `#equipe`, T3 `#constat` restructuré en 3 `.figure` éditables (`data-id` `cst-n1..3`/`cst-l1..3`), T4 fond marine + texture eau + chiffres Fraunces géants, T5 count-up une fois (`Motion.inView`, fallback IntersectionObserver + `prefers-reduced-motion`) + parallaxe fond, T6 `assets-manifest.md` à jour. Bug bloquant rencontré et corrigé en cours de route : le tableau `ORDER` de `arrange()` utilisait des ids inventés (`'manifeste'`, `'actions'`) au lieu des vrais `data-section-id` (`'mission'`, `'pillars'`) → condition de sortie jamais vraie → boucle infinie → gel du navigateur (d'où des timeouts d'automatisation). Vérif navigateur OK (figures, `data-id`, `saveAll()` propre, ordre re-forcé, console propre, capture desktop conforme). **Point ouvert non résolu à la clôture de la session** : Melvin a signalé que les 3 chiffres du `#constat` (8/10 éco-anxiété, 1% surface océans, ×3 pollution plastique 2040) ne sont pas ceux évoqués initialement — désaccord sur la source à date malgré une origine tracée (3 puces du `#constat` original + maquette validée + spec §4). Nouvelle proposition de chiffres à faire côté agent avant de patcher `#constat`.
- 2026-09-02 : Chiffres `#constat` remplacés par le triptyque validé par Melvin — **1 %** de la surface des océans mais mer la plus polluée du monde / **10 %** de la biodiversité marine mondiale / **+20 %** de réchauffement vs reste des océans (100 % Méditerranée, aligné sur le titre édité par l'équipe). `data-id` inchangés, count-up/parallaxe/console vérifiés OK. Commit `36c7400` sur `phase2-constat`, fast-forward sur `master`. **Phase 2 · Plan 1 complet.**
- 2026-09-02 : **Phase 2 terminée** — 3 sections restantes livrées + arc de plongée complet. Carnet de bord (galerie horizontale pinnée `Motion.scroll`/`sticky`, 5 cartes escale + parallaxe média/texte, teaser Point Zéro intact restylé sombre), 3 Piliers (volets split-screen abysse `#0C1B33`, survol/focus élargit et révèle la phrase forte en Fraunces, rétraction CSS pure, tap sur tactile), Équipe & Partenaires (résurgence ivoire + cartes qui se retournent au survol — recto portrait + nom Caveat jaune / verso bio — écart assumé vs spec §7 initiale sur demande de Melvin, `renderTeam`/`renderCofond` pas touchés, zéro `data-id` dupliqué). Arc complet : constat → carnet/expédition (marine) → piliers (abysse) → approche (marine) → résurgence (dégradé) → équipe (ivoire). Éditeur (`saveAll`/`applyData`/`applyBlocOrder`/`applySectionOrder`/`arrange()`) vérifié intact, mode édition déplie les cartes flip, slots média posés et tracés dans `assets-manifest.md`. Branche `phase2-sections`, 7 commits sur `master`. À vérifier en vrai navigateur (pin horizontal Carnet + flip Équipe, le pane d'automation gèle `ScrollTimeline`).
- 2026-09-02 : Nouvelle spec reçue pour refondre la section "Notre Approche" — remplacer la grille actuelle de 3 cartes textuelles (sous-listes + bloc dictionnaire "Méliorisme") par un composant "Parcours à 3 temps" sur fond Bleu Marine `#142A4A` : sticky scroll/carousel horizontal, par bascule (01/02/03) photo/vidéo marine plein format + numéro géant (opacity 0.3) + titre + accroche poétique, transitions fade-in/slide-up. Textes des 3 bascules fournis (« Remettre la beauté au centre » / « Passer de l'anxiété à l'action » / « Nourrir une écologie du désir »). Travail pas encore démarré à la clôture de cette session — la spec parle de "Framer Motion" mais le projet utilise la lib **Motion** vanilla (pas de React), à clarifier si besoin au moment de l'implémentation.
- 2026-09-03 : Section « Notre Approche » refondue en composant `.bascule-stack` (`position:sticky`, 3 bascules qui défilent une à une via `Motion.scroll`, crossfade fade+slide-up), 9 sous-items et le bloc dico Méliorisme supprimés, nouveaux `data-id` (`bascule-1..3-h`/`-cap`) pour ne pas écraser d'anciennes valeurs éditeur. Commit `e20be97`, mergé sur `master` (fast-forward, 21 commits d'avance sur `origin/master`, toujours non déployé). Phase 1 (Hero + Manifeste) migrée du driver scroll-event/rAF vanilla vers `Motion.scroll` (ScrollTimeline), fallback rAF conservé si `Motion` absent (commit `0f63863`). Transition sombre→turquoise→clair retravaillée pour s'étaler sur 4 zones enchaînées (bas Piliers → Approche → résurgence allongée 60→88vh → haut Équipe) au lieu d'un dégradé unique de 60vh (commit `29dbc7a`), sur retour de Melvin que le turquoise cassait trop sec. Point ouvert non résolu : lisibilité au repos des 3 Piliers sans média/hover (2 options proposées, pas encore tranchées) ; le *feel* réel des animations scroll-linked (Hero/Manifeste/résurgence/Carnet/Approche) reste à confirmer dans un vrai navigateur, le pane d'automation gelant `ScrollTimeline`.
- 2026-09-03 : Ordre des sections verrouillé définitivement (arbitrage B) — drag-and-drop désactivé dans l'éditeur, `saveAll`/`loadAll` n'écrivent/n'appliquent plus `sectionOrder` dans le bin, `arrange()` reste l'unique source de vérité (poignées `≡` masquées, panneau éditeur affiche « 🔒 ordre fixe — cliquer pour y aller », les clics restent des raccourcis de nav). Couleur source de la section Mission changée de l'ivoire à `#102746` (cible marine `#142A4A` inchangée) : la transition au scroll devient un approfondissement plutôt qu'un plongeon ivoire→marine, titre/eyebrow/sous-titre passent en tons ivoire dès le repos pour rester lisibles.
- 2026-09-03 : Section « Expédition 2026 » animée avec Motion — ligne « Une campagne océanographique — STARECORSICA… » retirée, `.exp-rail` en fade+translation, titre/intro en reveal `y 30→0`, liste des « Trois Gestes » en stagger 0.15 + chevron translaté/turquoise au survol, fiche technique en scale+translate avec délai après le texte, badge « 15 jours en mer » en spring, `prefers-reduced-motion` + filet de sécurité si l'observer ne se déclenche pas. Fiche technique restylée en vraie carte (bordure+ombre turquoise au survol), respirations augmentées (gaps, padding).
- 2026-09-03 : Saga couleur de fond de la section Mission résolue — la dérive perçue venait de `.manifeste-bg` (photo océan à `opacity:.09` par-dessus le fond), désactivée pour obtenir un fond pur. Plusieurs essais de paire source/cible rejetés car sous le seuil de perception (`#102746`→`#142A4A` et `#0e2c4c`→`#142A4A`, écart ΔE≈3, invisible au scroll). Résolu par échantillonnage réel de la couleur en bas de la photo Hero (canvas) : source **`#0b2842`** → cible marine charte `#142A4A` (écart `+9/+2/+8`, le plus lisible), la section Mission « émerge » sans rupture depuis le Hero. Ajouts : fondu de jonction Hero→Mission (`.mission::before`, dégradé sur `clamp(90px,16vh,200px)`) + départ du virage de couleur retardé au scroll (tient la couleur source sur les 38 premiers % de la section, transition sur les 40% suivants). Image de fond `#constat-bg` (photo océan 7%) retirée pour homogénéiser. Rampe de dégradé continue posée sur toute la zone bleue (Mission→statsbar→Constat→Expédition→Piliers→Approche→Résurgence), suppression de la marche sèche `#142A4A→#0C1B33` entre Expédition et Piliers.
- 2026-09-03 : Demande de fin de session — faire une copie du site actuel en ligne dans un fichier html puis commit+push. Reprise après interruption (limite de session) : snapshot sauvegardé dans `snapshots/naecoexpedition.org_2026-09-03.html` et **committé en local**. Confirmé au passage : la version publique `naecoexpedition.org` est identique au bit près à `origin/master:index.html` — le domaine se déploie bien depuis `origin/master`. **Non pushé délibérément** : `master` est à ~39 commits d'avance sur `origin/master` (toute la refonte Phase 1+2), un push simple déploierait l'intégralité de la refonte inachevée en prod. Décision demandée à Melvin, pas encore tranchée : (A) ne rien pousser, (B) pousser uniquement une branche `backup/pre-refonte` avec le snapshot (sauvegarde sur GitHub sans rien déployer), (C) pousser `master` en entier (déploiement immédiat, irréversible côté public). Reco implicite de l'agent : B.
- 2026-09-03 : Audit du code demandé par Melvin (« le travail a été continué sur une autre session, regarde ce qui a été fait et ce qui pourrait être encore amélioré »). Bilan : travail jugé solide (garde-fous éditeur, `data-id` conservés, count-up une fois, dégradation propre). 7 points d'amélioration identifiés, par priorité : **(1)** rythme/pacing du milieu de page trop long et vide tant que les médias réels ne sont pas câblés (Carnet ~500vh + Approche 300vh + résurgence 88vh quasi sans contenu) ; **(2)** `splitWords` fait `el.textContent` au lieu de ne parcourir que les nœuds texte → un mot mis en gras dans `mh` via la toolbar saute visuellement à chaque re-split (JSONbin le garde, le DOM le reperd) ; **(3)** dérive specs/plans/code — spec Phase 2 §9 dit que `#approche` garde son style actuel (faux, refondu en sticky-scroll), et seul le plan Constat existe sur les 4 prévus (Piliers/Carnet/Équipe codés sans plan écrit) ; **(4)** fond de repli no-JS incohérent — `.mission` n'a aucun fond sans JS (le `#102746`/`#0b2842` n'est posé que sous `html.js`), contrairement à la spec §7.3 ; **(5)** ~40 lignes de CSS mort à nettoyer (`.constat-grid`, ancienne `.constat`, `.bascule-minis`/`-mini-*`/`-tag`, `.bascule-grid` dupliqué) ; **(6)** perf mineure — `will-change:transform` posé en permanence sur 7 éléments (7 couches GPU statiques), script Motion sans `defer` (~30 Ko bloquants, volontaire pour l'ordre d'exécution) ; **(7)** hygiène repo — `master` à +32 commits non poussés à l'époque de l'audit (refonte jamais vue en ligne ni testée sur le vrai domaine), branche `phase2-constat` restée sur un vieux commit, fichier média non suivi, et le garde-fou `withCleanMh` jamais vérifié face à un vrai `saveAll()` en conditions réelles. Priorités proposées par l'agent : vérifier le garde-fou éditeur en réel + pousser sur une preview Cloudflare, retravailler le pacing, corriger `splitWords` + le fond no-JS, nettoyer le CSS mort.
- 2026-09-04 : Snapshot HTML de la version prod pré-refonte sauvegardé (`snapshots/naecoexpedition.org_2026-09-03.html`, copie exacte de `naecoexpedition.org`, éditeur JSONbin inclus) et poussé isolément sur la branche `backup/pre-refonte` (un seul commit par-dessus `origin/master`, sans toucher à `master`/prod). Sur confirmation de Melvin, **refonte complète poussée en production** (`git push origin master`, `237d022..714aa5f`, 39 commits) — Cloudflare a redéployé, vérifié en ligne (marqueurs `resurgence`/`exp-badge`/`piliers-volets`/`flip-inner`, ~213 Ko). Le fichier snapshot est aussi resté sur `master` (donc accessible publiquement à `/snapshots/...html`, `noindex,nofollow`, inoffensif). Reste à vérifier côté Melvin : hard-refresh + test rapide de l'éditeur JSONbin en prod (connexion + sauvegarde).
- 2026-09-04 : Brainstorm démarré (skill brainstorming) pour ajouter des vidéos de fond thème marin sur les sections « cœur de plongée » (Constat, Expédition, Piliers), architecture pensée réutilisable pour étendre à d'autres sections plus tard. Décisions actées en cours de session : transition entre sections en **fondu croisé réel** (les deux vidéos se superposent à la jonction) ; vidéos activées **aussi sur mobile**, via des transformations Cloudinary responsives par taille d'écran (résolution/bitrate réduits, même fichier source — même principe que l'image Hero `w_2000`/`w_1080`) plutôt que d'exiger un upload mobile dédié par section ; chaque section aura son **propre champ vidéo éditable dans l'éditeur JSONbin** (upload Cloudinary + aperçu, comme le champ vidéo Mission existant), avec possibilité de livrer ça en second temps si couplé à une refonte du mode édition.
- 2026-09-04 : Brainstorm vidéos de fond conclu — après review d'une contre-proposition Gemini (filtre `saturate`/`contrast` uniforme retenu ; désactivation vidéo sous 768px et chemins vidéo locaux rejetés, contraires aux décisions déjà prises), le mécanisme de transition **change** : **voile-vers-marine** (chaque section fond vers le marine à ses bords) remplace le fondu croisé acté plus tôt dans la journée. Raisons : réutilise la rampe de dégradé déjà posée entre les sections bleues, plus robuste sur mobile (une seule vidéo décodée à la fois, pas deux), s'étend à de nouvelles sections sans recâblage de "joint" par paire, tolère des plans vidéo tournés indépendamment. Confirmé par Melvin : les vidéos seront des séquences découpées dans une même vidéo mère, avec un risque de désynchronisation au point de jonction si l'utilisateur scrolle en cours de séquence — le voile règle ce cas par construction (opacité à 0 pile à la jonction, peu importe où en est la boucle). Spec écrite et commitée : `docs/superpowers/specs/2026-09-04-video-fonds-sections-bleues-design.md` (3 sections, calque vidéo `sticky` par section — pas `absolute`, à cause de la hauteur gonflée d'Expédition par le Carnet de bord —, fallback complet si reduced-motion/connexion lente/slot vide, éditeur JSONbin explicitement hors scope pour l'instant). En attente de validation de Melvin avant le plan d'implémentation.
- 2026-09-04 : Spec vidéos de fond complétée (durée 8-12s en boucle parfaite, 12-15s pour Expédition ; format technique H.264/MP4, 1280×720 desktop・640-854×480 mobile, sans piste audio, <2-3 Mo desktop・<1-1.5 Mo mobile — le voile marine semi-transparent rend inutile toute finesse d'image supérieure), commitée. Plan d'implémentation rédigé (skill writing-plans) puis exécuté en mode inline : calque vidéo `sticky` (100vh) + voile-vers-marine par section pour Constat/Expédition/Piliers, ancien `.constat-bg` (image morte + parallaxe JS) supprimé, nouveau module JS `IntersectionObserver` (charge/joue/pause à l'approche de la section, respecte `prefers-reduced-motion` et connexion lente), correctifs `overflow:hidden`/`position:relative` nécessaires au `sticky`. Implémenté et commité (`0530cc0`) : styles calculés vérifiés (sticky, marges négatives, z-index, dégradés), hit-test réel confirme le contenu (chiffres, carnet, volets) cliquable au-dessus des vidéos, console propre. Rendu visuel du fondu au scroll à confirmer par Melvin (le pane d'automation ne peut pas peindre un scroll temps réel de façon fiable). `VIDEO_BG_SLOTS` posés mais vides — en attente des 3 URLs Cloudinary (Constat, Expédition, Piliers).
- 2026-09-04 : Section Mission — oubli identifié dans le spec initial (aucune vidéo de fond ciblée alors qu'elle a deux mécanismes existants : encart vidéo à côté du texte + fond qui change de couleur au scroll `#0b2842`→`#142A4A`). Vidéo fournie (« Intro, baleine, solastalgie », `C:\Users\LENOVO\Videos\Naeco site video\`), uploadée sur Cloudinary. Implémentation en cours : suppression de la règle CSS morte `.manifeste-bg { will-change: transform }`, ajout des règles vidéo Mission dans le bloc CSS « Vidéos de fond » existant, nettoyage JS (`manifesteFrame` — retrait du lerp de couleur/`is-deep`/parallaxe `missionBg`, conservation du text-reveal) et câblage du slot `mission-video`.
- 2026-09-04 : Graphify (graphe de code tree-sitter, voir `03_knowledge/outils.md`) installé et activé sur ce repo — graphe minuscule (4 nœuds, quasi pas de code réel dans ce repo statique). `.claude/settings.json` renommé en `.claude/settings.local.json` + gitignored (le hook contient un chemin `graphify.exe` propre à la machine de Melvin) — commits `abb8d97` (`.gitignore`/`CLAUDE.md`) et `a9e83ba` (le reste).
- 2026-09-04 : Section Mission finalisée — migrée vers le même mécanisme plein-cadre (vidéo `sticky` + voile-vers-marine) que Constat/Expédition/Piliers, vidéo Cloudinary câblée (`w_1280` desktop, `w_640`+`q_auto:eco` mobile), ancien mécanisme (fond couleur au scroll + encart vidéo à côté du texte) supprimé, nettoyage CSS mort (`.manifeste-bg`, `is-deep`). Vérifié en navigateur (styles calculés + console propre), commit `4c52d1d`. Reste Constat/Expédition/Piliers à câbler dès réception des URLs Cloudinary.
- 2026-09-04 : Section Constat câblée avec la vidéo « tortue sous-marine » (voile-vers-marine, même mécanisme sticky) — vérifiée en navigateur, commit `16a64f2`. Reste Expédition et Piliers. Tentative séparée d'ajout d'une vidéo NAECO (film brut 23min34s, 792 Mo, dossier « more ») en boucle dans la section Mission — upload Cloudinary échoué (413, trop volumineux), identifié comme inadapté à un autoplay/loop silencieux (c'est un documentaire, pas une boucle ambiante) ; arbitrage à faire avec Melvin.
- 2026-09-04 (suite) : Fondus ffmpeg en entrée/sortie ajoutés sur les vidéos d'ambiance déjà en ligne (Mission, Constat) pour masquer le raccord de boucle — commit `e49f600`, fichiers un peu plus légers en prime. Compression du film NAECO en 720p (~92 Mo) lancée en arrière-plan ; en attendant, vidéo « baleine drone » posée en bouche-trou temporaire sur le slot Mission (commit `7a5d947`). Bug détecté et corrigé : une bande de vidéo brute était visible entre Hero et Mission — le fondu de jonction avait été supprimé en reconstruisant Mission pour la vidéo de fond, restauré au-dessus des nouveaux calques (commit `4602466`).
- 2026-09-04 (suite) : Deux bugs racines trouvés et corrigés (commit `e2b791f`). **(1)** Les calques vidéo `sticky` débordaient de la fin réelle des sections courtes (Mission/Constat/Piliers) — le voile s'arrêtant pile à la fin de section, une bande de vidéo brute non teintée filtrait entre deux sections. Fix : ces 3 sections repassées en position `absolute` calée exactement sur le voile (Expédition garde `sticky`, nettement plus haute à cause du Carnet de bord). **(2)** Les URLs de transformation Cloudinary à la volée (`f_auto,q_auto,w_...`) tronquaient la durée des vidéos livrées sur ce compte — le film NAECO de 23 min ressortait à 4,5s livrées, les vidéos d'ambiance à moins d'1s. Fix : tous les fichiers vidéo repassés en pré-encodé local (2 résolutions desktop/mobile), uploadés sans transformation à la livraison. **État final à la clôture** : Constat sur la vidéo raie manta (la tortue part sur le Carnet de bord, en fond translucide des cartes désormais réduites de 5 à 4 blocs — bloc « 2026 » retiré, redondant avec le teaser en dessous) ; bloc vidéo Mission câblé avec le film NAECO complet (23min34, réencodé 720p), en lecture normale avec contrôles (pas autoplay/loop). Reste à câbler : vidéos Expédition et Piliers (`VIDEO_BG_SLOTS`).

- 2026-09-04 : Cartes du Carnet de bord enrichies avec le vrai contenu par expédition (titre réel, résumé une ligne condensé depuis « Le sillage » sur `/expeditions`, légende poétique réduite à l'année seule sur demande de Melvin, bouton « ▶ Voir le film ») — vérifié en navigateur desktop et mobile (pas de débordement). Commits `09818a1` puis `ba2f445`. Tâche de compression du film NAECO (23 min → 720p) relancée par erreur après une confusion sur son état (perdue lors d'une interruption de session précédente), puis stoppée sur indication de Melvin : déjà traitée ailleurs.
- 2026-09-04 : Vidéo Cloudinary (`corail noir poissons.mp4`, encodée en 2 résolutions) câblée pour la section Expédition 2026, vérifiée en navigateur, commit `0fd75b4`. Les 4 sections « cœur de plongée » ont désormais leur vidéo de fond (Mission : baleine drone, Constat : raie manta, Expédition : corail noir/poissons, Carnet de bord : tortue) — il ne reste plus que **Piliers** dans le lot initial. Nouvelle demande reçue en fin de session (pas encore traitée) : basculer Expédition 2026 sur `corail noir plongeur.mp4`, poser `corail noir poissons.mp4` sur Piliers, et ajouter de la transparence sur la carte Pilier.
- 2026-09-05 : Demande de la veille traitée — Expédition 2026 basculée sur `corail noir plongeur.mp4` (nouvelle vidéo), Piliers récupère `corail noir poissons.mp4` (réutilisée, pas re-uploadée). Commit `c6e60b0`. Toutes les sections « cœur de plongée » ont maintenant leur vidéo. Cartes Piliers rendues translucides pour laisser voir la vidéo derrière, voile d'assombrissement allégé (jusqu'à 90% → 55-60% max) et flou masquant la vidéo retiré — commit `227621d`. Question ouverte de Melvin sur un effet de jonction (dégradé/flou) entre sections, probablement déjà résolu par ce correctif de transparence — à confirmer par Melvin après hard-refresh.
- 2026-09-05 (suite) : Cartes du Carnet de bord — champ lieu retiré des vignettes (année seule affichée), en corrigeant au passage un bug JSONbin qui réécrasait les éditions HTML avec les anciennes valeurs `carnet-N-place` à chaque chargement. Texte de la carte 2023 simplifié (retrait de « de la Corse »), commit `d1a08f0`, synchronisé sur JSONbin.
- 2026-09-05 (suite) : Deux sessions parallèles ont continué sur les jonctions Hero→Mission / Carnet→Expédition / transparence Piliers. Première passe : fondus renforcés par bandes dégradées (commit `16f5c41`). Deuxième passe (root-cause, après avoir repéré les commits de la session parallèle) : 3 causes réelles identifiées et corrigées — voile Hero plafonné à `.55` d'opacité (fondu déplacé côté Hero, `.mission::before` supprimé devenu inutile), voile Carnet en `absolute` mal calé par rapport au conteneur `sticky` (déplacé sur `.carnet::after`, ancré au contenu), fond opaque `--g2` sur `.img-area` recouvrant les cartes Piliers (remis transparent) — commit `f719d3f`. **Retour de Melvin après ce correctif : toujours pas bon** ("le flou est sur hero mais pas entre les 2 sections ça va pas du tout") — diagnostic complémentaire entamé (le fondu reste quasi transparent sur presque toute sa hauteur puis bascule d'un coup) mais interrompu par la limite de session, non résolu. En parallèle, vidéo "posidonie poisson" ajoutée en fond de "Notre approche" (thème vert/turquoise, dégradé au scroll conservé par-dessus) — commit `22ce4a4`, pas encore pushé. Nouvelle demande de Melvin restée sans réponse (session limit) : passer "Notre approche" en sticky, fondu Action→Approche comme Carnet→Expédition, rétablir le voile vert dégradé manquant, dégradé turquoise→ivoire après la section.
- 2026-09-05 (suite) : Section "Notre approche" — les 4 points demandés traités (commits jusqu'à `354fadd`, rien poussé) : passage en sticky via `clip-path` (au lieu d'`overflow:hidden`, qui cassait le pin des bascules), fondu Notre action→Notre approche ajouté (même technique que Carnet→Expédition 2026), bug du voile vert dégradé corrigé (`position:relative` manquant sur `.bascules`, le calque `.video-overlay` se dimensionnait sur toute la page au lieu de la section) — vérifié visuellement (navy→turquoise net), dégradé turquoise→ivoire de fin de section (`.resurgence`) restauré — il n'était pas cassé, juste masqué par le débordement de la vidéo sticky (`margin-bottom:-100vh`) réglé par le `clip-path` — vérifié. Nouvelle demande en fin de session : traiter la jonction haute Piliers → Approche avec la technique d'aplat `#0f2536` (comme Hero → Mission) — démarré, pas confirmé.
- 2026-09-05 (suite) : Diagnostic des jonctions Hero→Mission / Carnet→Expédition repris avec une nouvelle technique (aplat de couleur pleine sur le dernier tiers de la bande de jonction, au lieu d'un dégradé pur qui laissait transparaître la teinte turquoise du bas du Hero) — commits `f523ddb`, `4a180f7` — puis étendue à la jonction Constat→Carnet (`.constat::after`/`.carnet::before` en aplat commun) et aux bandes latérales sans voile du teaser Expédition 2026 (l'ancien calque de jonction était posé sur `.wrap`, contraint à 1160px, ne couvrait pas toute la largeur sur grand écran — remplacé par `.carnet::after` plein cadre) — commit `efcc2f2`. Non confirmé visuellement (panneau de preview gelé, fenêtre Claude en arrière-plan) — à valider par Melvin après hard-refresh.
- 2026-09-05 (suite) : Fond de la section Mission changé une nouvelle fois — la vidéo temporaire "baleine drone" remplacée par "Intro, baleine, solastalgie" (réencodée en 2 résolutions desktop/mobile, fondu 0.5s in/out, sans audio), uploadée sur Cloudinary via le preset non-signé `ml_default` du compte `dv3srxdmo` (pas besoin d'identifiants, contrairement à l'hypothèse initiale de l'agent) et câblée en URL brute pour éviter le bug de troncature des transformations à la volée. Bloc citation « Naeco choisit de célébrer la beauté de la mer… » + mention « Journal L'Équipe » supprimé de la section Mission. Vérifié en navigateur (durée complète 10,4s, lecture OK, sélection desktop/mobile correcte, console propre). Rien commité ni pushé.
- 2026-09-05 (suite) : Bug signalé — fine bande sans voile après la section "Notre approche" (jonction `#approche`→`.resurgence`), seule jonction du fichier sans bande de raccord. Cause identifiée : le voile vidéo de `.bascules` (`.video-overlay`) n'atteint le plein `#1f5a63` qu'à son tout dernier pixel (rampe 80%→100%), laissant transparaître la vidéo de fond sur ~100px avant le bord, alors que `.resurgence` démarre sur un aplat mort. Fix appliqué : `html.js .bascules::after`, aplat `#1f5a63` calqué sur `.pillars::after`. **Fix non concluant** — retour de Melvin : la bande est toujours visible. Diagnostic à reprendre.
- 2026-09-05 (suite) : **Bug de la bande sans voile résolu.** Vraie cause : pas la rampe du voile, mais le bord de raccord entre deux dégradés CSS rastérisés séparément — à DPR 1,5 les deux calques "arrivent"/"repartent" tous deux de `#1f5a63`, créant un filet clair d'1px pile sur la ligne de jonction (le `.bascules::after` du fix précédent ne pouvait rien y faire, coupé net par le `clip-path: inset(0)` de `.bascules`). Fix qui marche : `.resurgence::before`, un aplat opaque qui déborde de 3px au-dessus de la frontière et fond ensuite dans `.resurgence` — un seul calque continu recouvre le bord des deux dégradés. Vérifié visuellement dans Chrome à trois positions de scroll, bande disparue. Réglages fins additionnels : voile de jonction Hero→Mission (`.hero::after`) réduit (`clamp(280px,46vh,480px)` → `clamp(180px,30vh,320px)`), et marge au-dessus de "Notre mission" supprimée (`padding-top: 0` ciblé sur `.mission`, écart Hero→titre 112px→56px).
- 2026-09-05 (suite) : Bloc vidéo Mission restauré à sa position d'origine après suppression du bloc citation (`margin-top: 44px → 194px`), puis longue itération sur le cadrage/comportement de la vidéo de fond Mission. Bug de débordement diagnostiqué : la vidéo sticky (`margin-bottom:-100vh`) reste épinglée un écran de trop après la fin d'une section courte comme Mission (~1,5 écran), et rien ne la borne (`.mission` en `overflow:visible`, `.statsbar` suivante en `position:static`) → la vidéo + le voile bavaient sur la Stats bar. Plusieurs itérations (border le calque en `overflow:hidden`, ce qui masque la vidéo en fin de section ; agrandir la section en absorbant la Stats bar dedans) avant la solution finale : Stats bar déplacée à l'intérieur de `#manifeste` (HTML + `arrange()` + CSS) et `overflow: clip` (au lieu de `hidden`, qui empêchait le `position:sticky` de l'enfant vidéo de s'accrocher — `clip` découpe sans créer de conteneur de défilement) sur le calque Mission. Résultat vérifié à 1920×1080, 1440×900, 375×812 : section 2,1-2,4× l'écran, vidéo épinglée ~50-56% du défilement puis libérée, couverture vidéo 100% du visible à tout moment, 0px de débordement sur `.constat` suivante.
- 2026-09-05 (suite) : Vidéo de fond ajoutée à la section Équipe (`equipe.mp4`, ré-encodée 2 résolutions, boucle muette fondu 0,5s, même pattern que les 6 autres sections vidéo) avec voile ivoire semi-transparent (opacité ajustée 88%→50% sur retour de Melvin), passée en sticky. Bug de débordement sur `.partenaires` (même famille de bug que Mission/Approche — calque sticky non borné) corrigé via `clip-path: inset(0)` sur `.equipe` (même fix qu'Approche, commit `354fadd`). Grand voile dégradé ivoire→transparent ajouté en haut de section pour fondu progressif avec `.resurgence` précédente. Cadrage vidéo ajusté (`object-position: center 72%`).
- 2026-09-05 (suite) : Restructuration des cartes Équipe démarrée — les 3 sous-blocs (Co-fondateurs, nouveau bloc Bureau, Équipe expédition) unifiés sur la même grille/gabarit que "Équipe expédition" (cartes identiques 205×273 desktop, responsive 2 col/1 col). Nouveau bloc "Bureau" remplace "Présidente" : 2 cartes invisibles + 3 visibles (astuce CSS pour garder la colonne de gauche libre, alignée sur l'équipe), Anaëlle Marot devient 1re carte Bureau, éditable dans le panneau live (`renderBureau`, `DEFAULT_LISTS.bureau`). Équipe expédition réduite à 5 (doublon Michel Marengo retiré des defaults). **Non finalisé** : le contenu réel vient de JSONbin (`naeco_v5`), pas des defaults — la liste sauvegardée a encore 6 membres dont un doublon "Marion Fritsch" antérieur, à nettoyer par Melvin dans le panneau live.
- 2026-09-05 (suite) : Fond de la section Mission changé une nouvelle fois — vidéo "Intro, baleine, solastalgie" remplacée par "intro, mer, bulles" (réencodée en 2 résolutions desktop/mobile `mission2-d`/`mission2-m`, fondu 0,5s in/out, sans audio, `+faststart`), uploadée sur Cloudinary (`naeco-site/`), câblée en URL brute dans le slot `mission-video` — URLs vérifiées live (HTTP 200, durée complète 10,33s, pas de troncature, range requests OK). Ancienne vidéo laissée sur Cloudinary, non référencée. Rendu navigateur non confirmé (Browser pane masqué), à valider par Melvin.
- 2026-09-05 (suite) : Trois corrections sur la section Équipe — vidéo de fond qui démarrait trop haut (`object-position` inefficace car le recadrage `cover` se fait sur les côtés, remplacé par `transform: translateY(14vh)`) ; libellé "Bureau" réaligné sur la 1re carte visible via `.bureau-head` calé sur la même grille (`grid-column: 3`) ; dernière carte Équipe expédition en marge corrigée par une case invisible `.tcard.team-spacer` en fin de grille. Chloé Vancaeyzeele retirée des defaults (Michel Marengo restauré → 5 membres). Rien commité. Toujours pendant : JSONbin `naeco_v5` a encore 6 membres avec un doublon "Marion Fritsch" — à nettoyer par Melvin dans le panneau live.

## Liens
- Client : [[naeco]]
- Patterns utilisés : [[jsonbin-source-de-verite]]
- Repo / deployment : https://github.com/NAECOEXPEDITION/naeco-site (Cloudflare Pages)
