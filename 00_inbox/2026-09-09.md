---
tags: [daily]
created: 2026-09-09
---

# 2026-09-09

## Pense-bêtes

- [ ]

## Ce qu'on a fait

- naeco-site : tous les commits locaux pending poussés sur `master` (commit `34cc839`, 7 fichiers ; `master` synchro `origin/master`, working tree clean) — section carte interactive plein écran + titre sur 1 ligne, nav « Actions » → « Programme » sur les 7 pages, retouches manifeste/copie, + 4 commits antérieurs (charte graphique, compteurs stats) ; un message de commit malformé (heredoc PowerShell mal interprété) corrigé au passage via amend + push `--force-with-lease`
- naeco-site : demande « 3 actions => 3 gestes » traitée — libellé renommé « 3 gestes, une même vague », bouton « Découvrir nos expéditions » déplacé de la fiche technique vers la colonne droite sous la liste des 3 gestes, espacement ajouté ; vérifié en DOM à 1440px, non commité
- naeco-site : fix de la dérive du texte du HERO au dézoom implémenté (option 1 — `.hero` en `aspect-ratio`, textes repositionnés en `top:%` au lieu de `translateY` absolu) — vérifié en DOM à 1280px et 1920px (position stable à 80% de la hauteur du hero, contre ~235px de dérive avant), confirmé qu'aucune autre section n'a besoin du même traitement
- naeco-carte : design du tracking live du catamaran (expédition 2026) approfondi et spec finalisée/commitée (`docs/superpowers/specs/2026-09-09-live-tracking-catamaran-design.md`) — schéma D1 (table `positions`) + 3 endpoints Worker (`/ingest`, `/track`, `/track.gpx`) détaillés ; intégration à la carte existante (`index.html`) vérifiée dans le code : quasi purement additive (couche `lyr.live`, toggle générique, style dash-flow et polylineDecorator déjà présents), aucune nouvelle dépendance ; passage au plan d'implémentation démarré (skill writing-plans), plan pas encore produit dans la session
- NAECO : accès Notion connecté côté claude.ai — workspace « QG — Espace de travail de ML Cipriani » exploré (bases Projets, Ressources, MAJ contenus, Documentation Association, Accès Comptes, Contact) ; recherche de la plaquette NAECO 2025 infructueuse (0 résultat mot-clé exact dans le workspace, Drive non connecté)
- naeco-carte : plan d'implémentation du tracking live catamaran produit (6 tâches TDD, skill writing-plans) et exécution démarrée inline — Task 1 (core Worker) ✓ 20/20 tests, Task 2 (export GPX) ✓ 24/24 tests, Task 3 (routeur Worker) ✓ 39/39 tests, Task 4 (scaffold déploiement + smoke D1) ✓ D1 local + smoke d'intégration OK (déploiement distant `wrangler d1 create` reste à faire, requiert login Cloudflare), Task 5 (couche `lyr.live` dans la carte) démarrée ; travail fait directement sur `main`, commits par tâche, rien poussé
- naeco-site : chiffres statsbar mis à jour — 1ʳᵉ colonne 4 → 5, libellé « Expéditions réalisées » → « Expéditions & documentaires réalisés » (accord « réalisés » retenu par l'agent, sur la règle d'accord du nom masculin le plus proche — à confirmer par Melvin s'il préfère « réalisées »), compteur animé vérifié
- naeco-carte : Task 5 (couche `lyr.live`) terminée et vérifiée navigateur (polyline animée, marqueur orienté au cap, popup, append incrémental 8→10 points, note « pas de signal », toggle Live, masquage en mode édition) — les 6 tâches du plan tracking live catamaran sont complètes, 43/43 tests verts. Poussé en prod sur `main` (`aa64cd8..fd005c0`) sur demande explicite de Melvin (« déploie maintenant », puis « débrouille-toi pour push toi-même ») — Cloudflare Pages redéploie la carte automatiquement, le bouton Live y est visible. Reste hors périmètre agent (requiert le login Cloudflare interactif de Melvin) : `wrangler login` + `d1 create` + `secret put` + `deploy` pour activer le Worker `naeco-track` — tant que ce n'est pas fait, le bouton Live affiche « mise à jour indisponible » (dégradation prévue, rien de cassé). Configuration Traccar Client sur le smartphone clarifiée avec Melvin : URL corrigée en `https://track.naecoexpedition.org/ingest?token=...` (manquait `/ingest` et le token), fréquence réglée à 120s.
- naeco-carte : Worker `naeco-track` déployé en prod (`wrangler login` + `d1 create` + `secret put INGEST_TOKEN` + `wrangler deploy`) — testé de bout en bout (401 sur mauvais token, 200 + stockage sur bon token, `/track` renvoie le point, purge D1 remote OK). Custom domain `track.naecoexpedition.org` abandonné (zone Cloudflare pas rattachée au bon compte) — carte pointée directement sur l'URL `.workers.dev`, fonctionnellement équivalent. Tracking live catamaran opérationnel en prod, premiers points reçus du téléphone (test réel près d'Ajaccio).
- naeco-carte : bug de flash/saccade de la carte en mode Live diagnostiqué et corrigé — `transition:transform` sur `.boat-marker` entrait en conflit avec les transforms Leaflet appliqués à chaque frame de pan/zoom ; fix déployé et vérifié en prod (`931b201`), aucune erreur console.

## Décisions prises

- naeco-site : dérive du texte du HERO au dézoom diagnostiquée (hauteur fixe + `background-size:cover` + textes calés en `translateY` absolu) — option retenue : passer `.hero` en `aspect-ratio` + repositionner les textes en `top:%` pour que `cover` ne recadre plus l'image ; **implémenté et vérifié** (voir Ce qu'on a fait) — aucune autre section n'a besoin du même fix
- naeco-carte : approche retenue pour la position live du catamaran (expédition 2026) — smartphone à bord (Traccar Client/OwnTracks) → Cloudflare Worker + D1 (même stack que l'existant, gratuit), trace conservée et rejouable, figée dans JSONbin comme une expédition normale en fin de campagne ; implémentation démarrée (Task 1 core Worker ✓, Task 2 export GPX ✓, Task 3 routeur Worker en cours — voir Ce qu'on a fait)

## Next actions

- [ ] naeco-site : diagnostic jonctions Hero→Mission / Carnet→Expédition repris avec la technique d'aplat opaque (commits `f523ddb`, `4a180f7`) après échec du correctif root-cause `f719d3f` ; même technique étendue à Constat→Carnet et aux bandes latérales sans voile d'Expédition 2026 (commit `efcc2f2`) — non confirmé visuellement (preview gelée, fenêtre en arrière-plan), à valider par Melvin après hard-refresh
- [ ] naeco-site : jonction haute Piliers → Approche — appliquer la même technique d'aplat `#0f2536` que Hero → Mission (démarré en fin de session, à vérifier)
- [ ] naeco-site : pusher les commits locaux en attente (`22ce4a4` vidéo "posidonie poisson", `354fadd` Approche sticky/fondu/voile, `f523ddb`/`4a180f7`/`efcc2f2` jonctions, et tous les commits plus récents de la journée) une fois validés par Melvin
- [ ] naeco-site : pusher les commits locaux en attente (`22ce4a4` vidéo "posidonie poisson", `354fadd` Approche sticky/fondu/voile, `f523ddb`/`4a180f7`/`efcc2f2` jonctions, retouches « 3 gestes » et chiffres statsbar de la journée, et tous les commits plus récents) une fois validés par Melvin
- [ ] naeco-site : équipe — synchroniser/nettoyer la liste JSONbin `naeco_v5` (retirer le doublon "Marion Fritsch", ajouter le contenu réel du bloc Bureau) avant de considérer la restructuration des cartes Équipe terminée

## Blocages

-

## Liens

- [[naeco-site]]
- [[naeco-carte]]

## Notes vrac

-
