---
tags: [daily]
created: 2026-09-07
---

# 2026-09-07 — Push des commits naeco en attente (site + carte)

## Pense-bêtes

- [ ]

## Ce qu'on a fait

- naeco-carte : commit `aa64cd8` poussé — `.gitignore` (ignore `graphify-out/` + `.claude/settings.local.json`) et `CLAUDE.md` (règles graphify)
- naeco-site : les 26 commits locaux en attente poussés sur `origin/master` (dont `e022b28`) — inclut `22ce4a4` (vidéo "posidonie poisson"), `354fadd` (Approche sticky/fondu/voile), `f523ddb`/`4a180f7`/`efcc2f2` (jonctions) et tous les commits plus récents ; contenu : section Équipe (vidéo de fond + sous-bloc Bureau), statsbar intégrée à `#manifeste`, raccords Mission/Approche, nouvelles sources Cloudinary, `.claude/launch.json`, `more/` ajouté au `.gitignore`
- naeco-site : titres de section repassés en turquoise `#3DC1B3` (10 règles CSS sur 4 bases + 6 surcharges qui déviaient), puis les sections à fond ivoire clair (Équipe, Co-fondateurs, Bureau, Équipe expédition 2025, Partenaires, Personnalités qui soutiennent) restaurées en vert foncé `#12796d` sur retour de Melvin, pour garder le contraste — rien commité
- naeco-site : vidéo longue de la section Mission remplacée par `teaser_mare_nostrum`, bug corrigé (clic sur le bloc vidéo hors mode édition ouvrait le panneau d'édition — `.img-area-overlay` passé en `pointer-events:none` hors édition, `auto` en mode édition) — commit+push `ff7178b`
- naeco-site : voile/dégradé de la section Équipe retouché pour lisser la bande ivoire visible entre vidéo et fond clair (fond `.equipe` et `::before` repassés en rampe continue) — retour de Melvin en fin de session : la frontière est toujours visible, pas résolu
- naeco-site : voile de la section Équipe retravaillé en fondu alpha directement sur la vidéo (`mask-image`, dégradé `transparent → opaque` sur ~46vh) plutôt qu'un simple voile ivoire par-dessus — supprime le bord net visible ; retour de Melvin : dégradé bon mais masque désormais le haut de la vidéo
- naeco-site : correctif appliqué (option retenue par Melvin) — `translateY` de la vidéo 14vh → 26vh (dégage le haut du cadre de la zone de fondu) + `mask` raccourci 46vh → 16vh (ne feutre que la couture) + `::before` réduit 78vh → 60vh ; rendu non capturé côté agent (preview gelée), à confirmer visuellement par Melvin
- naeco-site : section Constat — chiffres clés repositionnés en diagonale (1 % à gauche, 10 % au centre, +20 % à droite) par CSS ; taille réduite en deux itérations (`clamp(72px,16vw,190px)` → `clamp(56px,10vw,120px)` → `clamp(44px,7.5vw,88px)`) et espacement vertical augmenté (jusqu'à `44vh` desktop / `36vh` mobile) sur retours successifs de Melvin
- naeco-site : section Expédition 2026 compactée pour tenir sur un écran 1366×768 (zoom 100%, sans scroll) — paddings/gaps réduits sur `.expedition`/`.exp-teaser`/`.exp-body`/`.exp-facts`/`.exp-chev` ; libellé « Trois gestes, une même vague » retiré (la liste Observer/Pister/Transmettre reste, sans intitulé) ; vérifié : 815px/642px/640px de hauteur sur 1440×900, 1366×700, 1280×640

## Décisions prises

- naeco-site : `more/` (vidéo source `NAECO (1080p).mp4` de 791 Mo + logo) exclu du versioning Git via `.gitignore` — GitHub refuse les fichiers >100 Mo ; passer par Git LFS ou un stockage externe si un jour besoin de versionner cette vidéo

## Next actions

- [ ] naeco-site : diagnostic jonctions Hero→Mission / Carnet→Expédition repris avec la technique d'aplat opaque (commits `f523ddb`, `4a180f7`) après échec du correctif root-cause `f719d3f` ; même technique étendue à Constat→Carnet et aux bandes latérales sans voile d'Expédition 2026 (commit `efcc2f2`) — non confirmé visuellement (preview gelée, fenêtre en arrière-plan), à valider par Melvin après hard-refresh
- [ ] naeco-site : jonction haute Piliers → Approche — appliquer la même technique d'aplat `#0f2536` que Hero → Mission (démarré en fin de session, à vérifier)
- [ ] naeco-site : pusher les commits locaux en attente (`22ce4a4` vidéo "posidonie poisson", `354fadd` Approche sticky/fondu/voile, `f523ddb`/`4a180f7`/`efcc2f2` jonctions, et tous les commits plus récents de la journée) une fois validés par Melvin
- [ ] naeco-site : équipe — synchroniser/nettoyer la liste JSONbin `naeco_v5` (retirer le doublon "Marion Fritsch", ajouter le contenu réel du bloc Bureau) avant de considérer la restructuration des cartes Équipe terminée
- [ ] naeco-site : le commit `ff7178b` a embarqué par erreur les changements de couleur d'eyebrow (turquoise) d'une session parallèle qui éditait `index.html` en même temps — à valider avec Melvin que c'est correct tel quel
- [ ] naeco-site : section Équipe — valider visuellement le fondu vidéo/voile après le correctif option 3 (mask alpha raccourci à 16vh + vidéo translateY 26vh + `::before` réduit à 60vh) ; agent n'a pas pu capturer le rendu (preview gelée), boutons de réglage prêts si la couture réapparaît ou si le haut de vidéo reste trop voilé
- [ ] naeco-site : renommer la section "Constat" en "Contexte" ; section "Notre action" — au survol d'une carte elle passe en grand (existant, ne pas toucher), les 2 autres cartes passent à 75% de transparence

## Blocages

-

## Liens

- [[naeco]]
- [[naeco-site]]
- [[naeco-carte]]

## Notes vrac

- Git : un fichier binaire lourd (>100 Mo, ex. vidéo source) ne se committe jamais tel quel — l'ajouter au `.gitignore` et passer par Git LFS ou un stockage externe. Un `git push` avec un blob >100 Mo est rejeté par GitHub.
