---
tags: [daily]
created: 2026-08-30
---

# 2026-08-30

## Pense-bêtes

- [ ]

## Ce qu'on a fait

- Bug critique résolu : le hook d'ingestion `/clear` → agent headless `good-night` ne s'était en réalité **jamais déclenché automatiquement** (les logs du 29 venaient de lancements manuels du script). Root cause : sur ce build Windows (Claude Code 2.1.199), `/clear` n'émet pas `source="clear"` sur `SessionStart` — tout matcher `"clear"` exact était mort-né, et les hooks `SessionStart` reçoivent un stdin vide (le garde applicatif dans le script échouait systématiquement). Basculé le hook sur `SessionEnd` avec matcher en alternation (`clear|logout|prompt_input_exit|other`), retiré la dépendance à stdin du script `ingest_on_clear.sh`. Chaîne d'ingestion continue vérifiée fonctionnelle (cette ingestion même en est la preuve). Voir [[troubleshooting]].
- RAG Millenium : la masterclass *Claude Code + Obsidian – Alexis* (77 chunks) était enrichie mais jamais passée par `embed_chunks.py` → invisible dans la recherche vectorielle. Corrigé (embeddée, backup DB posé, vérifiée via `query_rag.py`). Normalisé 2 chunks avec label module mal accentué (`Formation Productivité` → `Formation Productivite`). Nettoyé 2 next actions erronées reportées à tort dans la note du 30 (résidu obsolète, pas de vraie tâche "373 leçons restantes").
- esprit-docker : repo local resynchronisé avec GitHub (5 commits récupérés depuis l'autre PC de Morgane/l'équipe — auth admin PBKDF2 tentée puis reverte vers `ADMIN_PASSWORD` en Secret Cloudflare). Fast-forward propre, rien à commit/push. Recommandation en attente de confirmation : ajouter `.claude/settings.local.json` au `.gitignore` du repo client.
- Installé en global : skill Claude Code `muller-brockmann-grid-systems` (grille modulaire Swiss/International Typographic Style — scaffold `grid_tokens.py` + harness de vérification Puppeteer `verify_grid.js`) et le package npm `motion` (ex-Framer Motion, `motion@13.1.1`). Outils disponibles pour les prochains projets web/landing (voir [[outils]]).

## Décisions prises

- Hook d'ingestion vault : `SessionEnd` (pas `SessionStart`) est l'événement fiable pour capter `/clear` sur ce build Windows — `SessionStart` n'émet pas `source="clear"` ici malgré la doc.

## Next actions

- [x] Tester `/clear` et vérifier qu'un log apparaît dans `~/.claude/skills/good-night/state/logs/` ✅ 2026-08-30
- [x] Lancer un premier `/good-night` en fin de journée ✅ 2026-08-30
- [ ] Renseigner la couche commerciale des fiches clients (origine lead, TJM, devis, échéances)
- [ ] Signaler à NAECO la clé JSONbin + mot de passe éditeur en clair dans leur repo
- [x] Vérifier que le hook d'ingestion /clear fonctionne bout en bout (commit attendu dans le vault) après le fix OAuth + --dangerously-skip-permissions ✅ 2026-08-30
- [ ] Configurer le MCP motion (demande faite, laissée en suspens avant `/clear`)

## Blocages

-

## Liens

- [[esprit-docker]]
- [[troubleshooting]]

## Notes vrac

-
